## Simple Example

This is a short paragraph with `inline code` and **bold text**.

This second paragraph keeps the fixture plain so optional renderers stay off.

- One **important** item
- One item with `code`
